# Copy to .env.prod on the OCI host. Never commit the populated file.
GHCR_OWNER=sanjayshanmugap
CRIA_IMAGE_TAG=v0.1.0

# A DNS name whose A record points to the OCI reserved public IPv4 address.
CRIA_HOST=cria.example.duckdns.org
CADDY_EMAIL=you@example.com

RUST_LOG=info
