global:
  scrape_interval: 15s

scrape_configs:
  - job_name: control-plane
    static_configs:
      - targets: ["control-plane:9090"]

  - job_name: workers
    dns_sd_configs:
      - names: ["worker"]
        type: A
        port: 9100
        refresh_interval: 10s
